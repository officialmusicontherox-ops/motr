import ChartsView from "@/components/ChartsView";

export const metadata = {
  title: "Charts — MOTR",
  description: "The tracks listeners backed hardest this week and this month on MOTR.",
};

export default function ChartsPage() {
  return <ChartsView />;
}
