node_modules/
.claude/settings.local.json

# Secrets — never commit. Live Stripe keys live in client/.env
.env
.env.*
!.env.example

# Database exports contain emails and password hashes — never commit one.
motr-backup-*.json
*.sql
